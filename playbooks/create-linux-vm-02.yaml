- name: "Create Ubuntu 20.04 VM with Docker (fixed public IP)"
  hosts: localhost
  connection: local
  tasks:

    - name: "Ensure virtual network exists (shared)"
      azure_rm_virtualnetwork:
        name: vnetDockerDemo
        resource_group: rgUbuntuSoutheastAsia
        address_prefixes: "10.10.0.0/16"
        location: southeastasia
        state: present

    - name: "Create new subnet"
      azure_rm_subnet:
        name: subnetDockerDemo01
        resource_group: rgUbuntuSoutheastAsia
        virtual_network: vnetDockerDemo
        address_prefix: "10.10.1.0/24"
        state: present

    - name: "Create new public IP (Static)"
      azure_rm_publicipaddress:
        name: pipDockerDemo01
        resource_group: rgUbuntuSoutheastAsia
        allocation_method: Static     # ✅ ใช้แบบ static ไม่ต้องระบุ IP ตรง
        state: present

    - name: "Create new NIC (Fixed private IP optional)"
      azure_rm_networkinterface:
        name: nicDockerDemo01
        resource_group: rgUbuntuSoutheastAsia
        virtual_network: vnetDockerDemo
        subnet: subnetDockerDemo01
        public_ip_name: pipDockerDemo01
        # ถ้าต้องการฟิก private IP เพิ่ม:
        # ip_configurations:
        #   - name: ipconfig1
        #     private_ip_address: 10.10.1.100
        #     private_ip_allocation_method: Static
        state: present

    - name: "Create new VM with Docker installed"
      azure_rm_virtualmachine:
        name: vmDockerDemo01
        resource_group: rgUbuntuSoutheastAsia
        location: southeastasia
        vm_size: Standard_B1s
        admin_username: azureuser
        ssh_password_enabled: false
        managed_disk_type: Standard_LRS
        ssh_public_keys:
          - path: /home/azureuser/.ssh/authorized_keys
            key_data: "{{ lookup('file', '~/.ssh/id_rsa.pub') }}"
        network_interfaces: nicDockerDemo01
        image:
          offer: 0001-com-ubuntu-server-focal
          publisher: Canonical
          sku: 20_04-lts
          version: latest
        custom_data: |
          #!/bin/bash
          apt-get update -y
          apt-get install -y curl
          curl -fsSL https://get.docker.com -o get-docker.sh
          sh get-docker.sh
          usermod -aG docker azureuser
        state: present  